# Quick Task 260511-92x: Reorganizar navegação staff/provider

**Created:** 2026-05-11
**Mode:** quick
**Status:** Ready for execution

## Goal

Consolidar a navegação staff/provider: remover nav item "Gestão" (placeholder), reorganizar "Alunos" com role branching (staff = lista alunos direto, provider = 2 tabs Alunos/Staff), e setar universalblackout1@gmail.com como provider no seed.

---

## Plan 1: Consolidar navegação e seed provider

### Task 1: Remover nav "Gestão" e ajustar shell

**files:** `mobile/lib/features/staff/screens/staff_shell.dart`
**action:** Remove the "Gestão" nav item (index 5) from `_destinations`, `_railDestinations`, `_currentIndex`, and `_onTap`. Reindex "Alunos" from 6→5. Remove the import/reference to `staffGestao` route path.
**verify:** Shell compiles, only 6 nav items remain (Painel, Agenda, Chats, Docs, Recursos, Alunos)
**done:** No more "Gestão" icon in nav

### Task 2: Provider tab view no StaffCadastroScreen

**files:** `mobile/lib/features/staff/screens/staff_cadastro_screen.dart`, `mobile/lib/features/staff/screens/staff_gestao_screen.dart`
**action:** 
1. In `StaffCadastroScreen.build()`, add role check — if provider, return a `DefaultTabController` with 2 tabs: "Alunos" (current screen content) + "Staff" (`_StaffListTab` from gestao). Extract current student list body into a `_StudentListTab` widget.
2. Move `_StaffListTab`, `_StaffCard`, `_FilterTab` (gestao versions) into `staff_cadastro_screen.dart` or import from gestao. The simplest approach: import `staff_gestao_screen.dart` classes OR inline them.
3. For staff role: screen renders current student list directly (no tabs), same as today.
**verify:** Provider sees 2 tabs (Alunos/Staff) like client resources pattern; staff sees only student list
**done:** Role-based screen branching works

### Task 3: Atualizar seed — provider role

**files:** `backend/scripts/seed.py`
**action:** Change `"role": "secretary"` to `"role": "provider"` for `universalblackout1@gmail.com` in `STAFF_DATA`.
**verify:** Seed script validates (role is in CHECK constraint: staff, coordinator, secretary, provider)
**done:** universalblackout1@gmail.com is the sole provider account

### Task 4: Limpar router e route_names

**files:** `mobile/lib/core/router/app_router.dart`, `mobile/lib/core/router/route_names.dart`
**action:** Remove the `staffGestao` GoRoute from app_router.dart. Remove `staffGestao` from RouteNames and RoutePaths. Remove any unused imports (StaffGestaoScreen).
**verify:** App compiles, no dead routes
**done:** Clean router without gestão route
