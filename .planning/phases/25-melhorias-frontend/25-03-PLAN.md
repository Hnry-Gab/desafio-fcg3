---
phase: 25-melhorias-frontend
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/shared/utils/date_utils.dart
  - mobile/lib/features/client/screens/client_home_screen.dart
  - mobile/lib/features/client/screens/client_documents_screen.dart
  - mobile/lib/features/client/screens/client_chat_screen.dart
  - mobile/lib/features/client/screens/client_chat_detail_screen.dart
  - mobile/lib/features/client/screens/client_notifications_screen.dart
  - mobile/lib/features/client/screens/widgets/document_detail_sheet.dart
  - mobile/lib/features/client/screens/widgets/appointment_detail_sheet.dart
  - mobile/lib/features/staff/screens/staff_documents_screen.dart
  - mobile/lib/features/staff/screens/staff_appointment_detail_screen.dart
  - mobile/lib/features/staff/screens/staff_ai_screen.dart
  - mobile/lib/features/staff/screens/staff_chat_detail_screen.dart
  - mobile/lib/features/staff/screens/staff_chats_screen.dart
  - mobile/lib/features/staff/screens/staff_intervention_screen.dart
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
must_haves:
  truths:
    - "All date/time display uses shared utility functions from date_utils.dart"
    - "No inline _formatDate, _formatDateTime, or _relativeTime functions remain in screen files"
    - "Dates in current year show DD/MM format; other years show DD/MM/YYYY"
    - "Relative time shows short form (agora, 5m, 2h, 3d) up to 7 days"
  artifacts:
    - path: "mobile/lib/shared/utils/date_utils.dart"
      provides: "Centralized date formatting utilities"
      exports: ["formatDate", "formatDateTime", "formatRelativeTime"]
      min_lines: 30
  key_links:
    - from: "mobile/lib/features/client/screens/client_home_screen.dart"
      to: "mobile/lib/shared/utils/date_utils.dart"
      via: "import shared/utils/date_utils.dart"
      pattern: "import.*date_utils"
    - from: "mobile/lib/features/staff/screens/staff_chats_screen.dart"
      to: "mobile/lib/shared/utils/date_utils.dart"
      via: "import shared/utils/date_utils.dart"
      pattern: "import.*date_utils"
---

<objective>
Create a shared date formatting utility and refactor 13 screen files to eliminate 12+ duplicated inline date formatting functions.

Purpose: Currently every screen defines its own `_formatDate`, `_formatDateTime`, or `_relativeTime` function with slightly different implementations. This creates inconsistency and maintenance burden. Centralizing into a shared utility ensures uniform date display across the app.
Output: New `date_utils.dart` shared utility + 13 screen files refactored to use it.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-melhorias-frontend/25-CONTEXT.md

@mobile/lib/features/client/screens/client_home_screen.dart
@mobile/lib/features/client/screens/client_documents_screen.dart
@mobile/lib/features/client/screens/client_chat_screen.dart
@mobile/lib/features/client/screens/client_chat_detail_screen.dart
@mobile/lib/features/client/screens/client_notifications_screen.dart
@mobile/lib/features/client/screens/widgets/document_detail_sheet.dart
@mobile/lib/features/client/screens/widgets/appointment_detail_sheet.dart
@mobile/lib/features/staff/screens/staff_documents_screen.dart
@mobile/lib/features/staff/screens/staff_appointment_detail_screen.dart
@mobile/lib/features/staff/screens/staff_ai_screen.dart
@mobile/lib/features/staff/screens/staff_chat_detail_screen.dart
@mobile/lib/features/staff/screens/staff_chats_screen.dart
@mobile/lib/features/staff/screens/staff_intervention_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared date_utils.dart utility</name>
  <files>mobile/lib/shared/utils/date_utils.dart</files>
  <read_first>
    - mobile/lib/features/client/screens/client_notifications_screen.dart (line ~411: _formatRelativeTime — most complete relative time impl)
    - mobile/lib/features/client/screens/client_documents_screen.dart (line ~378: _formatDateTime — DD/MM/YYYY HH:MM pattern)
    - mobile/lib/features/client/screens/client_home_screen.dart (line ~22: _formatDateTime — DD/MM HH:MM pattern)
    - mobile/lib/features/client/screens/client_chat_screen.dart (line ~663: _formatDate — DD/MM HH:MM pattern)
    - mobile/lib/features/staff/screens/staff_chats_screen.dart (line ~460: _relativeTime)
    - .planning/phases/25-melhorias-frontend/25-CONTEXT.md (D-05 through D-09)
  </read_first>
  <action>
Create directory `mobile/lib/shared/utils/` (it doesn't exist yet).

Create file `mobile/lib/shared/utils/date_utils.dart` with these functions:

```dart
/// Centralized date formatting utilities for Alpha Connect.
///
/// All screens should import this file instead of defining inline
/// formatting functions. Follows D-05 through D-09 conventions.

/// Format a DateTime as a date string.
/// Current year: DD/MM. Different year: DD/MM/YYYY.
/// Per D-07.
String formatDate(DateTime date) {
  final day = date.day.toString().padLeft(2, '0');
  final month = date.month.toString().padLeft(2, '0');
  final now = DateTime.now();
  if (date.year == now.year) {
    return '$day/$month';
  }
  return '$day/$month/${date.year}';
}

/// Format a DateTime as date + time string.
/// Current year: DD/MM HH:MM. Different year: DD/MM/YYYY HH:MM.
/// Per D-07.
String formatDateTime(DateTime date) {
  final day = date.day.toString().padLeft(2, '0');
  final month = date.month.toString().padLeft(2, '0');
  final hour = date.hour.toString().padLeft(2, '0');
  final minute = date.minute.toString().padLeft(2, '0');
  final now = DateTime.now();
  if (date.year == now.year) {
    return '$day/$month $hour:$minute';
  }
  return '$day/$month/${date.year} $hour:$minute';
}

/// Format a DateTime as relative time (short form).
/// < 1 min: "agora", < 60 min: "Xm", < 24h: "Xh", ≤ 7 days: "Xd".
/// Beyond 7 days: falls back to formatDate().
/// Per D-08, D-09.
String formatRelativeTime(DateTime timestamp) {
  final now = DateTime.now();
  final diff = now.difference(timestamp);

  if (diff.inMinutes < 1) return 'agora';
  if (diff.inHours < 1) return '${diff.inMinutes}m';
  if (diff.inDays < 1) return '${diff.inHours}h';
  if (diff.inDays <= 7) return '${diff.inDays}d';

  return formatDate(timestamp);
}
```

Key rules per CONTEXT decisions:
- D-06: No new package dependency — use manual `padLeft` implementation
- D-07: Omit year if current year, show full DD/MM/YYYY if different year
- D-08: Short form vocabulary: "agora", "5m", "2h", "3d"
- D-09: Relative-to-absolute threshold at 7 days
  </action>
  <verify>
    <automated>test -f mobile/lib/shared/utils/date_utils.dart && grep "String formatDate" mobile/lib/shared/utils/date_utils.dart && grep "String formatDateTime" mobile/lib/shared/utils/date_utils.dart && grep "String formatRelativeTime" mobile/lib/shared/utils/date_utils.dart && echo "PASS: all 3 functions exist"</automated>
  </verify>
  <acceptance_criteria>
    - File `mobile/lib/shared/utils/date_utils.dart` exists
    - Contains `String formatDate(DateTime date)` function
    - Contains `String formatDateTime(DateTime date)` function
    - Contains `String formatRelativeTime(DateTime timestamp)` function
    - Uses `padLeft(2, '0')` for zero-padding (no external package per D-06)
    - Contains year comparison logic: `date.year == now.year` (per D-07)
    - Contains threshold `diff.inDays <= 7` (per D-09)
    - Contains short form strings: `'agora'`, `'m'`, `'h'`, `'d'` (per D-08)
    - `flutter analyze` on the file shows no issues
  </acceptance_criteria>
  <done>Shared date_utils.dart created with formatDate, formatDateTime, and formatRelativeTime functions following all CONTEXT decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor all screens to use shared date_utils.dart</name>
  <files>mobile/lib/features/client/screens/client_home_screen.dart, mobile/lib/features/client/screens/client_documents_screen.dart, mobile/lib/features/client/screens/client_chat_screen.dart, mobile/lib/features/client/screens/client_chat_detail_screen.dart, mobile/lib/features/client/screens/client_notifications_screen.dart, mobile/lib/features/client/screens/widgets/document_detail_sheet.dart, mobile/lib/features/client/screens/widgets/appointment_detail_sheet.dart, mobile/lib/features/staff/screens/staff_documents_screen.dart, mobile/lib/features/staff/screens/staff_appointment_detail_screen.dart, mobile/lib/features/staff/screens/staff_ai_screen.dart, mobile/lib/features/staff/screens/staff_chat_detail_screen.dart, mobile/lib/features/staff/screens/staff_chats_screen.dart, mobile/lib/features/staff/screens/staff_intervention_screen.dart</files>
  <read_first>
    - mobile/lib/shared/utils/date_utils.dart (the shared utility just created in Task 1)
    - mobile/lib/features/client/screens/client_home_screen.dart (line ~22: top-level _formatDateTime function)
    - mobile/lib/features/client/screens/client_documents_screen.dart (line ~378: _formatDateTime method)
    - mobile/lib/features/client/screens/client_chat_screen.dart (line ~663: _formatDate method)
    - mobile/lib/features/client/screens/client_chat_detail_screen.dart (line ~253: _formatDate method)
    - mobile/lib/features/client/screens/client_notifications_screen.dart (line ~411: top-level _formatRelativeTime)
    - mobile/lib/features/client/screens/widgets/document_detail_sheet.dart (line ~100: _formatDateTime)
    - mobile/lib/features/client/screens/widgets/appointment_detail_sheet.dart (line ~120: _formatDateTime)
    - mobile/lib/features/staff/screens/staff_documents_screen.dart (line ~393: _formatDateTime, line ~650: _formatDate)
    - mobile/lib/features/staff/screens/staff_appointment_detail_screen.dart (line ~223: _formatDate)
    - mobile/lib/features/staff/screens/staff_ai_screen.dart (line ~493: _formatDate)
    - mobile/lib/features/staff/screens/staff_chat_detail_screen.dart (line ~176: _formatDateTime)
    - mobile/lib/features/staff/screens/staff_chats_screen.dart (line ~460: _relativeTime)
    - mobile/lib/features/staff/screens/staff_intervention_screen.dart (line ~332: _relativeTime)
  </read_first>
  <action>
For EACH of the 13 files below, perform these two operations:

**Step A: Add import** — Add at the top of the file (after existing imports):
```dart
import '../../../shared/utils/date_utils.dart';
```
Note: adjust the relative path based on file depth:
- Files in `features/client/screens/`: `import '../../../shared/utils/date_utils.dart';`
- Files in `features/client/screens/widgets/`: `import '../../../../shared/utils/date_utils.dart';`
- Files in `features/staff/screens/`: `import '../../../shared/utils/date_utils.dart';`

**Step B: Delete the inline function** and replace all call sites with the shared utility function.

**Mapping of inline functions → shared utility replacements:**

1. **client_home_screen.dart** (line ~22):
   - DELETE: Top-level `String _formatDateTime(DateTime dt) { ... }` function
   - REPLACE calls: `_formatDateTime(...)` → `formatDateTime(...)`

2. **client_documents_screen.dart** (line ~378):
   - DELETE: Method `String _formatDateTime(DateTime date) { ... }` inside the state class
   - REPLACE calls: `_formatDateTime(document.requestedAt)` → `formatDateTime(document.requestedAt)` (used at line ~312 and ~378)

3. **client_chat_screen.dart** (line ~663):
   - DELETE: Method `String _formatDate(DateTime dt) { ... }` inside the state class
   - REPLACE calls: `_formatDate(session.startedAt)` → `formatDate(session.startedAt)` (used at lines ~713, ~723)

4. **client_chat_detail_screen.dart** (line ~253):
   - DELETE: Method `String _formatDate(DateTime dt) { ... }` inside the state class
   - REPLACE calls: `_formatDate(log.createdAt)` → `formatDate(log.createdAt)` (used at line ~276)

5. **client_notifications_screen.dart** (line ~411):
   - DELETE: Top-level `String _formatRelativeTime(DateTime timestamp) { ... }` function
   - REPLACE calls: `_formatRelativeTime(notification.timestamp)` → `formatRelativeTime(notification.timestamp)` (used at line ~301)

6. **document_detail_sheet.dart** (line ~100):
   - DELETE: Method `String _formatDateTime(DateTime date) { ... }` inside the state class
   - REPLACE calls: `_formatDateTime(document.requestedAt)` → `formatDateTime(document.requestedAt)` (used at lines ~70, ~75)

7. **appointment_detail_sheet.dart** (line ~120):
   - DELETE: Method `String _formatDateTime(DateTime date) { ... }` inside the state class
   - REPLACE calls: `_formatDateTime(appointment.createdAt)` → `formatDateTime(appointment.createdAt)` (used at line ~98)

8. **staff_documents_screen.dart** (lines ~393 and ~650):
   - DELETE: Method `String _formatDateTime(DateTime date) { ... }` (line ~393)
   - DELETE: Method `String _formatDate(DateTime date) { ... }` (line ~650)
   - REPLACE calls: `_formatDateTime(document.requestedAt)` → `formatDateTime(...)` (lines ~364, ~369)
   - REPLACE calls: `_formatDate(document.requestedAt)` → `formatDate(...)` (line ~605)

9. **staff_appointment_detail_screen.dart** (line ~223):
   - DELETE: Method `String _formatDate(DateTime date) { ... }` inside the state class
   - REPLACE calls: `_formatDate(appointment.createdAt)` → `formatDate(appointment.createdAt)` (line ~44)

10. **staff_ai_screen.dart** (line ~493):
    - DELETE: Method `String _formatDate(DateTime date) { ... }` inside the state class
    - REPLACE calls: `_formatDate(session.startedAt)` → `formatDate(session.startedAt)` (line ~455)

11. **staff_chat_detail_screen.dart** (line ~176):
    - DELETE: Method `String _formatDateTime(DateTime dt) { ... }` inside the state class
    - REPLACE calls: `_formatDateTime(session!.startedAt)` → `formatDateTime(session!.startedAt)` (line ~100)

12. **staff_chats_screen.dart** (line ~460):
    - DELETE: Method `String _relativeTime(DateTime dateTime) { ... }` inside the state class
    - REPLACE calls: `_relativeTime(item.date)` → `formatRelativeTime(item.date)` (line ~432)

13. **staff_intervention_screen.dart** (line ~332):
    - DELETE: Method `String _relativeTime(DateTime dateTime) { ... }` inside the state class
    - REPLACE calls: `_relativeTime(session.startedAt)` → `formatRelativeTime(session.startedAt)` (line ~278)

**DO NOT touch** `booking_flow_sheet.dart` `_formatDateHeader` — that function parses date *strings* (not DateTime objects) and has different semantics.

After all replacements, run `flutter analyze` to verify no compilation errors.
  </action>
  <verify>
    <automated>cd mobile && grep -rl "_formatDateTime\|_formatDate\b\|_relativeTime\|_formatRelativeTime" lib/features/client/screens/ lib/features/staff/screens/ lib/features/client/screens/widgets/ --include="*.dart" 2>/dev/null | grep -v "booking_flow_sheet" | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: no inline date functions remain" && grep -rl "import.*date_utils" lib/features/ --include="*.dart" | wc -l | xargs -I{} test {} -ge 13 && echo "PASS: 13+ files import shared utility"</automated>
  </verify>
  <acceptance_criteria>
    - Zero files in `lib/features/client/screens/` or `lib/features/staff/screens/` contain `_formatDateTime`, `_formatDate` (as method), `_relativeTime`, or `_formatRelativeTime` functions (excluding booking_flow_sheet.dart's `_formatDateHeader`)
    - 13 screen files contain `import.*date_utils.dart`
    - All call sites use `formatDate(...)`, `formatDateTime(...)`, or `formatRelativeTime(...)` (no underscore prefix)
    - `flutter analyze` shows no new errors or warnings
    - booking_flow_sheet.dart `_formatDateHeader` is untouched
  </acceptance_criteria>
  <done>All 13 screen files refactored to use shared date_utils.dart. Zero duplicated inline date formatting functions remain.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| DateTime input → formatted string | Date formatting functions receive DateTime objects from models; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-25-05 | Tampering | date_utils.dart | accept | Pure formatting functions with no side effects; DateTime values come from trusted model layer |

</threat_model>

<verification>
1. `flutter analyze` from `mobile/` — zero new issues
2. `grep -r "_formatDateTime\|_formatDate\b\|_relativeTime\|_formatRelativeTime" mobile/lib/features/ --include="*.dart"` — only `booking_flow_sheet.dart` matches (for `_formatDateHeader`)
3. `grep -r "import.*date_utils" mobile/lib/features/ --include="*.dart" | wc -l` — returns 13
4. `test -f mobile/lib/shared/utils/date_utils.dart` — file exists
</verification>

<success_criteria>
- Shared date_utils.dart exists with formatDate, formatDateTime, formatRelativeTime
- All 13 screen files import and use the shared utility
- Zero inline date formatting functions remain (except unrelated booking_flow_sheet)
- Date display is consistent: DD/MM for current year, DD/MM/YYYY otherwise
- Relative time uses short form: agora, Xm, Xh, Xd (up to 7 days)
- No compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-melhorias-frontend/25-03-SUMMARY.md`
</output>
