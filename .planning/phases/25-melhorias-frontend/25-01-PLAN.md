---
phase: 25-melhorias-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/core/theme/app_colors.dart
  - mobile/lib/core/theme/app_theme.dart
  - mobile/lib/features/client/screens/client_shell.dart
  - mobile/lib/shared/widgets/alpha_connect_logo.dart
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-18
  - D-19
  - D-20
  - D-21
must_haves:
  truths:
    - "Light mode primary color is #00695C (deep teal) providing ≥4.5:1 contrast on light surfaces"
    - "Dark mode primary remains #00E5FF unchanged"
    - "Desktop NavigationRail has exactly 4 destinations (no Suporte)"
    - "Logo displays adequately in both light and dark modes"
  artifacts:
    - path: "mobile/lib/core/theme/app_colors.dart"
      provides: "Updated light mode primary color"
      contains: "Color(0xFF00695C)"
    - path: "mobile/lib/features/client/screens/client_shell.dart"
      provides: "NavigationRail without Suporte destination"
  key_links:
    - from: "mobile/lib/core/theme/app_colors.dart"
      to: "mobile/lib/core/theme/app_theme.dart"
      via: "AppColors.primary referenced in _lightColorScheme"
      pattern: "AppColors\\.primary"
---

<objective>
Fix light mode contrast by changing primary color to WCAG AA compliant #00695C, remove ghost Suporte NavigationRail destination from desktop, and verify logo display in both modes.

Purpose: Improve accessibility and remove dead UI elements — light mode text/icons using primary color currently have ~1.5:1 contrast ratio (unreadable), and the desktop rail has 5 items but only 4 handlers.
Output: Theme files updated, client shell cleaned up, logo verified.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-melhorias-frontend/25-CONTEXT.md

@mobile/lib/core/theme/app_colors.dart
@mobile/lib/core/theme/app_theme.dart
@mobile/lib/features/client/screens/client_shell.dart
@mobile/lib/shared/widgets/alpha_connect_logo.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update light mode primary color to #00695C and clean up theme</name>
  <files>mobile/lib/core/theme/app_colors.dart, mobile/lib/core/theme/app_theme.dart</files>
  <read_first>
    - mobile/lib/core/theme/app_colors.dart (current color definitions — lines 9-41 light theme)
    - mobile/lib/core/theme/app_theme.dart (ColorScheme construction — lines 64-92 light, 124-260 light ThemeData)
    - .planning/phases/25-melhorias-frontend/25-CONTEXT.md (D-01 through D-04)
  </read_first>
  <action>
In `app_colors.dart`:
- Change line 10: `static const Color primary = Color(0xFF00E5FF);` → `static const Color primary = Color(0xFF00695C);`
- This is the ONLY change in app_colors.dart. Dark mode primary (`darkPrimary = Color(0xFF00E5FF)` at line 43) stays unchanged per D-02.
- The neon glow color `neonTealLight = Color(0xFF00838F)` at line 82 stays unchanged per D-03 — it's for glow effects, not the ColorScheme.

In `app_theme.dart`:
- The `_lightColorScheme` at line 64 references `AppColors.primary` which will automatically pick up the new `#00695C` value — no change needed in `_lightColorScheme` itself.
- However, check `focusedBorder` at line 151: `BorderSide(color: AppColors.primary, width: 2)` — this uses light primary for focused input borders in light mode. With `#00695C` this will be a deep teal border which is fine.
- Check `outlinedButtonTheme` at line 194: `BorderSide(color: AppColors.primary)` — same situation, will cascade correctly.
- Check `navigationBarTheme` light at lines 228-239: label text uses `color: AppColors.primary` for selected state (line 232) and `AppColors.onSurfaceVariant` for unselected (line 238). The selected label with `#00695C` will have good contrast.
- Check `floatingActionButtonTheme` light at line 249: `backgroundColor: AppColors.primary` — with `#00695C` the FAB will be deep teal with black text (`onPrimary`). The `onPrimary` is `#000000` — on `#00695C` background contrast is ~5.6:1 which passes WCAG AA. No change needed.
- Check `elevatedButtonTheme` light at line 162: `backgroundColor: AppColors.primary` + `foregroundColor: AppColors.onPrimary` (black text on `#00695C` background) — passes WCAG AA.

No changes needed in `app_theme.dart` — all references to `AppColors.primary` cascade automatically from the single change in `app_colors.dart`.
  </action>
  <verify>
    <automated>cd mobile && grep -n "Color(0xFF00695C)" lib/core/theme/app_colors.dart && grep -n "Color(0xFF00E5FF)" lib/core/theme/app_colors.dart | grep -c "darkPrimary\|neonTeal" | xargs -I{} test {} -ge 2 && echo "PASS: light primary changed, dark primary and neonTeal preserved"</automated>
  </verify>
  <acceptance_criteria>
    - app_colors.dart line 10 contains `Color(0xFF00695C)` (was `Color(0xFF00E5FF)`)
    - app_colors.dart line 43 still contains `darkPrimary = Color(0xFF00E5FF)` (unchanged)
    - app_colors.dart line 76 still contains `neonTeal = Color(0xFF00E5FF)` (unchanged)
    - app_colors.dart line 82 still contains `neonTealLight = Color(0xFF00838F)` (unchanged)
    - app_theme.dart is NOT modified (cascading handles everything)
    - `flutter analyze` shows no new issues from the color change
  </acceptance_criteria>
  <done>Light mode primary is #00695C with ≥4.5:1 contrast ratio on light surfaces. Dark mode and glow colors unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ghost Suporte NavigationRailDestination + verify logo</name>
  <files>mobile/lib/features/client/screens/client_shell.dart, mobile/lib/shared/widgets/alpha_connect_logo.dart</files>
  <read_first>
    - mobile/lib/features/client/screens/client_shell.dart (lines 66-92: _railDestinations list, lines 46-57: _onTap handler)
    - mobile/lib/shared/widgets/alpha_connect_logo.dart (logo widget — check brightness branching)
    - .planning/phases/25-melhorias-frontend/25-CONTEXT.md (D-18, D-19, D-20, D-21)
  </read_first>
  <action>
In `client_shell.dart`:
- Remove lines 87-91: the 5th `NavigationRailDestination` for "Suporte" (headset_mic icon):
  ```dart
  NavigationRailDestination(
    icon: Icon(Icons.headset_mic_outlined),
    selectedIcon: Icon(Icons.headset_mic),
    label: Text('Suporte'),
  ),
  ```
- The `_railDestinations` list should now have exactly 4 items (Início, Chat, Documentos, Recursos).
- Verify `_onTap` handler (lines 46-57) only handles cases 0-3 — it already does (`case 0` through `case 3`), so no change needed there.
- Verify `_currentIndex` (lines 37-43) — it maps to indices 0-3. The `clientSupport` route maps to `return 3` (fallback) which is fine — support is accessed via header icon, not rail.

For logo verification (D-20, D-21):
- Read `alpha_connect_logo.dart` to check brightness-adaptive behavior.
- Verify the widget uses `Theme.of(context).brightness` to select between light/dark SVG variants.
- If the logo already handles both modes correctly (which Phase 17 implemented), document "Logo verified — no changes needed" in the summary.
- If any contrast issue is found (e.g., logo invisible on light background), apply minimal fix consistent with Phase 17 decisions.
  </action>
  <verify>
    <automated>cd mobile && grep -c "NavigationRailDestination" lib/features/client/screens/client_shell.dart | xargs -I{} test {} -eq 4 && echo "PASS: exactly 4 rail destinations" && grep -c "headset_mic" lib/features/client/screens/client_shell.dart | xargs -I{} test {} -eq 0 && echo "PASS: no Suporte icon"</automated>
  </verify>
  <acceptance_criteria>
    - client_shell.dart `_railDestinations` list contains exactly 4 `NavigationRailDestination` entries
    - client_shell.dart contains zero references to `headset_mic` or `Suporte` in the rail destinations
    - client_shell.dart `_onTap` handler has cases 0-3 only (no dead case 4)
    - client_shell.dart `_destinations` (mobile nav) still has 4 items (unchanged)
    - alpha_connect_logo.dart reviewed — brightness-adaptive behavior confirmed
    - `flutter analyze` shows no new issues
  </acceptance_criteria>
  <done>Desktop NavigationRail has exactly 4 destinations. Suporte icon removed. Logo verified in both modes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Theme → All screens | Color change cascades app-wide; incorrect value affects all UI |
| Navigation index → Route | Removing rail item must not shift indices for remaining items |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-25-01 | Tampering | app_colors.dart primary | accept | Visual-only change; no security impact. Cascading via ColorScheme is deterministic. |
| T-25-02 | Denial of Service | NavigationRail index | mitigate | Verify _onTap handles only indices 0-3 after removal; assert _railDestinations.length matches _destinations.length (both 4) |

</threat_model>

<verification>
1. `cd mobile && flutter analyze` — zero new warnings/errors
2. `grep "Color(0xFF00695C)" lib/core/theme/app_colors.dart` returns the light primary
3. `grep -c "NavigationRailDestination" lib/features/client/screens/client_shell.dart` returns exactly 4
4. `grep "headset_mic" lib/features/client/screens/client_shell.dart` returns nothing
</verification>

<success_criteria>
- Light mode primary color is #00695C throughout the app (single source of truth in AppColors)
- Dark mode primary remains #00E5FF (untouched)
- Desktop navigation rail shows 4 items with no ghost Suporte entry
- Logo displays correctly in both light and dark modes
- No flutter analyze regressions
</success_criteria>

<output>
After completion, create `.planning/phases/25-melhorias-frontend/25-01-SUMMARY.md`
</output>
